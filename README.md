# Intersection-Observer-API-JS-web-app
This repo contains the src files for a web application made using HTML, CSS, JS and also using Intersection Observer API to animate the web app on-scroll. The Intersection Observer observes the html element passing through the observable section of the viewport at a particular moment.
